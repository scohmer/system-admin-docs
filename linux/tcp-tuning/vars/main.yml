---
target_hosts: all

# Tuning profile: web_server | database | storage | custom
tcp_tuning_profile: web_server

# Custom parameters (only used when tcp_tuning_profile == 'custom')
tcp_custom_params: {}
  # net.core.somaxconn: "1024"
  # net.ipv4.tcp_fin_timeout: "30"
